# JupyterLite build environment.
name: jupyterlite-env
channels:
  - https://repo.prefix.dev/emscripten-forge
  - https://repo.prefix.dev/conda-forge
dependencies:
  - pip
  - pip:
    - holonic
    - rdflib>=7.0.0
    - pyshacl>=0.26.0
